# Руководство по работе с репозиториями и ведению веток IMW

## 🧭 Общие принципы работы с ветками

При работе над любым функционалом создавайте ветку от текущей (`master`/`main`) с названием, отражающим суть разработки.

**Пример:**
- Вы работаете в репозитории `Unturned` и хотите реализовать функцию Chams.
- Создайте ветку с именем, соответствующим разрабатываемому функционалу:
  - `feat: create custom chams` — для новой функции
  - `fix: ...` — для исправления багов или доработок

## 🔄 Рабочий процесс (Feature Branch Workflow)

1.  **Создание ветки:** От ветки `master` создайте новую ветку для своей задачи.
2.  **Разработка:** Весь код, связанный с задачей, коммитится только в эту ветку.
3.  **Завершение разработки:**
    - ✅ Тщательно протестируйте весь функционал, чтобы ничего не сломалось.
    - 🔀 Создайте Pull Request (Merge Request) для слияния с `master`.
    - 🧹 **Перед мерджем**, если есть несколько коммитов, используйте **git squash**, чтобы объединить их в один логический коммит.
4.  **Слияние и закрытие:**
    - Смерджите ветку в `master`.
    - Удалите отработанную ветку из репозитория.
5.  **Релиз:** Создайте новый релиз на GitHub согласно инструкции ниже.

---

## 🚀 Инструкция по выпуску релиза (Versioning)

### 1. Создание релиза на GitHub
- Перейдите в раздел **Releases** на GitHub.
- Нажмите **"Create a new release"**.
- Заполните описание изменений.

### 2. Система версионирования (Semantic Versioning)
Версия имеет формат **`X.Y.Z`**, где:
- **`X` — Мажорная версия (Major).** Увеличивается на +1 при глобальных изменениях:
  > Пример: переработка архитектуры, смена движка, полный редизайн основного интерфейса.
  > При инкременте `X` сбрасываются `Y` и `Z` до 0 (v2.**0.0**).

- **`Y` — Минорная версия (Minor).** Увеличивается на +1 при добавлении серьезного, но не глобального функционала:
  > Пример: добавление нового модуля, крупной функции, исправление множества багов.
  > При инкременте `Y` сбрасывается `Z` до 0 (v1.2.**0**).

- **`Z` — Патч-версия (Patch).** Увеличивается на +1 при незначительных изменениях:
  > Пример: добавление мелкой функции, исправление одного бага, оптимизация кода.

### 3. Старт проекта
Если продукт выпускается впервые, установите ему версию **`v1.0.0`** и составьте подробное описание реализованной системы.

---

## ⚙️ Стенды разработки (Dev / Prod)

В проекте поддерживаются два основных стенда:

| Стенд | Назначение | Ключевые требования |
| :--- | :--- | :--- |
| **`dev`** | **Активная разработка и отладка.** | • Подробное логирование.<br> • Консоль отладки.<br> • Инструменты для разработчиков (хоткеи для тестов).<br> • Читаемая документация в коде.<br> • Упрощенная сборка и инжект для быстрого тестирования. |
| **`prod`** | **Финальная версия для использования.** | • Логирование отключено или минимально.<br> • Усиленные механизмы защиты (обфускация, анти-отладка).<br> • Убраны все инструменты для разработчиков.<br> • Максимальная стабильность и производительность. |

### Правила использования:
- Для `dev` и `prod` должны создаваться **раздельные релизы** с четким указанием стенда в описании (например, `[DEV] v1.2.3` или `[PROD] v1.2.3`).
- Если проект не подразумевает различий между отладочной и финальной версией, использование раздельных стендов **необязательно**.

---

## 📝 Краткий чек-лист для разработчика
- [ ] Создал ветку от `master` с понятным названием (`feat:` / `fix:`).
- [ ] Веду разработку только в своей ветке.
- [ ] Протестировал весь функционал перед мерджем.
- [ ] Объединил коммиты через `squash` (при необходимости).
- [ ] Создал Pull Request и получил аппрув (если требуется).
- [ ] Смерджил ветку в `master` и удалил ее.
- [ ] Определил тип изменений (Major/Minor/Patch) для новой версии.
- [ ] Создал релиз на GitHub с корректной версией и описанием.
- [ ] Указал в релизе, для какого стенда (`dev`/`prod`) он предназначен.
